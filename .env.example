# Database
# Runtime: pooled Neon connection injected by Vercel Marketplace.
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler:5432/turbolev?sslmode=require"
# Prisma migrations: direct/unpooled connection when Neon provides it.
DATABASE_URL_UNPOOLED="postgresql://USER:PASSWORD@HOST:5432/turbolev?sslmode=require"

# Binotel REST API
BINOTEL_COMPANY_ID=""
BINOTEL_API_KEY=""
BINOTEL_API_SECRET=""
BINOTEL_API_BASE_URL="https://api.binotel.com/api"
BINOTEL_API_VERSION="4.0"
BINOTEL_API_TIMEOUT_MS="10000"

# Optional additional protection for the public webhook URL.
# If set, configure Binotel webhook URL as:
# https://YOUR_DOMAIN/api/telephony/binotel-webhook?token=YOUR_RANDOM_TOKEN
BINOTEL_WEBHOOK_TOKEN=""

# Binotel WebSocket
BINOTEL_WS_KEY=""
BINOTEL_WS_SECRET=""
BINOTEL_WS_URL="wss://ws.binotel.com:9002"
